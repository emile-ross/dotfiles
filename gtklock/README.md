# gtklock configuration

The style.css file is used to configure the appearance of the lockscreen.  
Inside the style.css file, a path is specified to load the lockscreen image.  
You can change it to whatever you want.

The assets directory is used to store images or other assets used in the style.css file.  
